{% render 'partials/head.liquid' %}

{% comment %} <h1>Menu</h1>

  {% for dish in dishes %}
    {{ dish.name }}
    <img src="{{ dish.imagelink }}" alt="{{ dish.name }}" />
{% endfor %} {% endcomment %}


{% render 'partials/foot.liquid' %}